# TeamPulse

A lightweight internal dashboard that shows who's working on what.

## Running locally
Open `index.html` in a browser, or serve the folder with any static server.

# edit README.md, add a line noting the new search feature
# direct push test